name: TOC Generator

on:
  push:

jobs:
  generateTOC:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: technote-space/toc-generator@v4
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TOC_TITLE: '## 📚 知识库目录'
